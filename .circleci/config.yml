version: 2.1
orbs:
  ruby-rails: sul-dlss/ruby-rails@
workflows:
  build:
    jobs:
      - ruby-rails/lint:
          name: lint
      - ruby-rails/brakeman:
          name: brakeman
      # Using test-gem since no DB.
      - ruby-rails/test-gem:
          name: test
          context: dlss
